import { GridLoader } from "react-spinners";

export default function Spinner() {
    return(
        // SpeedMultipler က ပိုမြန်စေဖို့ပါ။
        <GridLoader color={'#1E3A8A'} speedMultiplier={2}/>
    )   
}